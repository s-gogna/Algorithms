/*!****************************************************************************
@file SelectionSort.cpp

Case     | Time Complexity
-------------------------------------------------------------------------------
Best     |  O(n^2)
Average  |  O(n^2)
Worst    |  O(n^2)
******************************************************************************/

template <typename T>
void SelectionSort(vector<T>& input)
{
	// For x in [0 to length-2]

		// Initialize minIndex to x

		// For y in [x to length-1]

			// If input[x] > input[y]

				// MinIndex is assigned y

		// If minIndex is not equal to x

			// Swap
}
